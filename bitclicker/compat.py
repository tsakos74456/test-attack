# -*- coding: utf-8 -*-
"""Python 3 compatibility shims kept for back-compat with the rest of the package."""

text_type = str
binary_type = bytes
string_types = (str,)
unicode = str
basestring = (str, bytes)
